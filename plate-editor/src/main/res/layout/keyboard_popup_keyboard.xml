<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@mipmap/keyboard_popup_panel_background"
    android:orientation="horizontal">

    <com.license.plate.keyboard.KeyboardView
        android:id="@android:id/keyboardView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        app:keyPreviewLayout="@layout/keyboard_key_preview"
        app:keyTextSize="22sp"
        app:popupLayout="@layout/keyboard_popup_keyboard" />

    <ImageButton
        android:id="@android:id/closeButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginStart="8dp"
        android:background="@color/transparent"
        android:clickable="true"
        android:src="@mipmap/ic_launcher" />
</LinearLayout>